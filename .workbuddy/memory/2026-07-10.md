# 2026-07-10

## 评审并修正 codebuddy-engineering 系列（docs/ai-coding/codebuddy-engineering）
应要求对《CodeBuddy 工程化实战系列》（README + 10 篇）做写作评审，并自行查证官方文档后修正事实错误。

### 核实到的真实事实（已对照官方文档）
- **SDK 包**：核心包 `@tencent-ai/agent-sdk`；但 `createSdkMcpServer`/`tool` 实际从子路径 `@tencent-ai/agent-sdk/mcp` 导入，`query` 从根包导入。工具用 `schema`+`handler`（非 `parameters`/`execute`）。**无 `createAgent`，官方用 `query({ prompt, options: { model, systemPrompt, mcpServers } })`**。要求 Agent SDK v0.1.24+，Preview。
- **MCP 配置**：项目级 `.mcp.json`（非 `.codebuddy/mcp_settings.json`），用户级 `~/.codebuddy/.mcp.json`。作用域 user/project/local（优先级 local>project>user）。传输 stdio/SSE/HTTP。确认有 `defer_loading`。
- **上下文文件**：CodeBuddy 真实文件名是 **`CODEBUDDY.md`**（项目根 `./CODEBUDDY.md` 或 `./.codebuddy/CODEBUDDY.md`，启动自动加载；用户级 `~/.codebuddy/CODEBUDDY.md`；模块化规则 `.codebuddy/rules/*.md`；`/init` 可生成；兼容 CLAUDE.md）。原 #04 完全没提机制，只讲内容。
- **企业版链接**：真实地址 `https://www.codebuddy.cn/docs/ide/Codebuddy-enterprise-edition/Codebuddy-enterprise-edition-Overview`（原 `tencentcloud.com/.../product/1256` 为错链接/国际站域名）。

### 已修正的文件
- `08-agent-sdk-custom-tools.md`：导入路径、`schema`/`handler`、`query` 替代 `createAgent`、footer 注明版本与子路径。
- `04-context-and-project-memory.md`：补 CODEBUDDY.md 机制说明 + footer。
- `06-mcp-integration.md`：配置路径改为 `.mcp.json`、作用域/传输类型更正、示例补 `type: stdio`。
- `09-enterprise-compliance.md`：企业链接改为 codebuddy.cn 官方。
- `10-pitfalls-and-boundaries.md`：标题 `XX 天` 占位符删除。
- `README.md`：目录中同步去掉 `XX 天`。

### 后续：#07/#10 anecdote 去重
用户确认（"ok"）执行去重。改 `10-pitfalls-and-boundaries.md`：
- 第 37 行（R1 慢且贵）：不再重述“补全等十秒”事件，改为边界结论 + 回指 [07-multi-model-strategy.md] 的“我踩过的坑”。
- 第 39 行（模型切换上下文断裂）：删去与 #07 第 48 行重复的“新模型不理解旧模型约定”细节，改为结论 + 回指 #07。
- #07 保持原样。`./07-multi-model-strategy.md` 内部链接已通过 check.sh 校验（2029 链接有效）。
- 校验全过。系列事实错误 + 重复 anecdote 两处均已解决；#05/#10 对 CLI 稳定性的重复未动（不在本次确认范围）。

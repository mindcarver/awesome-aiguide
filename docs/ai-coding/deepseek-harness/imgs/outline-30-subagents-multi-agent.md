---
article: 30-subagents-multi-agent.md
type: mixed
density: rich
style: notion
palette: default
image_count: 22
---

# 子 Agent 与多智能体配图大纲

1. 命名 provider 注册表与六种后端共存。
2. 一次式静态能力、可继续方法发现的分工。
3. 一次式 run 的启动、结果、dispose 生命周期。
4. stopReason、structured、diagnostic 的失败边界。
5. 可继续 Session、Activation、inbox、子 Activation 的结构。
6. running、waiting、settled 从运行事实推导。
7. followup 的同驻留、唤醒、冷恢复三路路由。
8. 授权只认活的直接父对象。
9. signal 只在 inbox 接受前有效。
10. interrupt 只停当前轮次且保留 FIFO。
11. report 与 settled 的作者、时机和用途对照。
12. 同一步骤兄弟委派并行与容量边界。
13. backgroundMode continuable 的后台优先默认。
14. 委派当刻策略快照与 approval never。
15. provider 只参与首次创建，冷恢复绕过 provider。
16. descriptor 提供子的持久身份。
17. Codex、Claude Code 的一次式产品 provider 与后台 Job。
18. Agent Teams 的 Lead、roster、mailbox、任务板。
19. 共享 cwd、writeScopes 与最终 diff 的协作边界。
20. 深度、工具过滤、人设三枚刻度旋钮。
21. listChildren 的 live-preferred 纯读阶梯。
22. 崩溃冷恢复与完整多轮协作路径。

---
illustration_id: 05
type: comparison
style: notion
generator: gpt-image-2
text_mode: required
---

插件的三种合法形态 — Comparison View

Layout: Three equal vertical cards across a wide 16:9 canvas, all converging into one shared apply(ctx) lifecycle line at the bottom. Keep the cards visually equivalent: function plugin is lightest, object plugin is middle, class/Service is richest.

CARDS:
- Card 1: “函数插件” with a simple function symbol and a small apply(ctx) mark.
- Card 2: “对象插件” with a small object card and apply(ctx).
- Card 3: “类插件” with a service box, the label “Service”, and apply(ctx).
- Bottom convergence: one shared runtime plug labeled “apply(ctx)”.

RELATIONSHIPS: Show that the three shapes enter the same Cordis lifecycle. Do not render source code blocks or extra API names.

LABELS: Title: “插件的三种合法形态”. Labels: “函数插件”, “对象插件”, “类插件”, “Service”, “apply(ctx)”. Takeaway: “一个服务就是一个插件”. Render exact supplied labels only; do not invent pseudo-text.

COLORS: White background (#FFFFFF); near-black outlines and text (#1A1A1A); pastel blue (#A8D4F0) for function; pastel yellow (#F9E79F) for object; pastel pink (#FADBD8) for class/Service; dark gray (#4A4A4A) convergence line.

STYLE: GPT Image 2 illustration in a polished Notion-like hand-drawn comparison style. Rounded cards, simple symbolic icons, single-weight outlines, slight wobble, generous whitespace, restrained pastel accents, no texture, no gradients, no logos, and no photorealism. Text is required, exact, large, prominent, readable, and article-specific. Render as a real PNG image, not SVG or programmatic vector artwork.

ASPECT: 16:9 landscape, medium information density.

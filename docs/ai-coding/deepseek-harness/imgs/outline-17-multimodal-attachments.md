---
type: mixed
density: rich
style: notion
generator: gpt-image-2
image_count: 12
---

# 多模态附件：配图大纲

1. 三种图片形态与模型只见请求版本。
2. 引用在日志、字节在存储的持久附件边界。
3. 准入与规范化的两道门。
4. 内容寻址持久化与先落盘后事件。
5. `read_image` 的统一生命周期与缩放坐标回乘。
6. `inputModalities` 三道模态门控。
7. 纯文本模型把历史图投影为确定性占位文本。
8. 按路由派生请求版本与缓存。
9. DeepSeek Files API 上传、过期和有限重试。
10. Files 失败后的内联 base64 有界回退。
11. 从最老图片开始的确定性请求降级。
12. 两级限额与持久历史不变的总体边界。

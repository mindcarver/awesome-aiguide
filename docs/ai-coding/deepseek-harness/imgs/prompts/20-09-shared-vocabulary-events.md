---
illustration_id: 20-09
type: communication
style: notion
generator: gpt-image-2
---

Notion 风格通信示意图，标题 `共享词汇表，发射方不依赖监听方`。左边 `ctx.fs` 发射信封事件 `fs/read`、`fs/write`、`fs/edit`；中间一本小词典 `事件名 + target + version`；右边两个独立监听卡 `观察策略` 与 `审计插件`。用虚线表示松耦合。白色背景、近黑线、淡蓝淡黄淡粉、标签清晰。

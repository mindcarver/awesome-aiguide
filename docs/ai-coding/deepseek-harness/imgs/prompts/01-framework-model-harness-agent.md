---
illustration_id: 01
type: framework
style: notion
generator: gpt-image-2
text_mode: required
---

模型 + Harness = Agent — Conceptual Framework

Layout: A clean left-to-right framework on a wide 16:9 canvas. Keep the main equation visually dominant and use three large rounded knowledge cards with generous whitespace: Model → Harness → Agent. Make the center Harness card visibly modular and the right Agent card visibly actionable.

ZONES:
- Zone 1, left: a compact pastel-blue card titled “模型”, with a simple hand-drawn neural-network / chip symbol and a small note that the model alone produces text.
- Zone 2, center and largest: a pastel-yellow modular card titled “DeepSeek Harness”, containing four small interlocking plugin tiles labeled “一切皆插件”, “Cordis”, “能力接缝”, and “可替换 provider”. Show the tiles as reversible plug-in modules, not as a monolithic core. Add small connector tags “ctx.llm”, “ctx.fs”, and “ctx.shell” around the center card.
- Zone 3, right: a pastel-pink card titled “Agent”, with simple line doodles for file access, shell execution, memory/session log, and approval/sandbox. Connect it from the center with several light arrows to show that the harness supplies the operating environment for action.
- Bottom strip: a small, high-contrast takeaway ribbon with the exact text “换 provider，换整个产品”. Use a subtle arrow or swap icon to communicate replaceability.
- Keep the composition conceptual and readable; do not render a literal robot, futuristic city, or dense software screenshot. No logos or brand marks.

LABELS: Title: “模型 + Harness = Agent”. Labels: “DeepSeek Harness”, “一切皆插件”, “Cordis”, “能力接缝”, “可替换 provider”. Takeaway: “换 provider，换整个产品”. Render these exact Chinese and Latin labels only; do not invent extra pseudo-text.

COLORS: Pure white background (#FFFFFF); near-black outlines and text (#1A1A1A); pastel blue (#A8D4F0) for the model card; pastel yellow (#F9E79F) for the central harness card; pastel pink (#FADBD8) for the agent card; dark gray (#4A4A4A) for secondary connectors. Use accents sparingly and preserve generous white space.

STYLE: GPT Image 2 illustration in a polished Notion-like editorial style. Clean hand-drawn line work with a slight intentional wobble, rounded modular cards, simple doodle icons, single-weight outlines, restrained pastel accents, no texture, no gradients, and no photorealism. Text is required: render one short title, the supplied labels, and the concise takeaway in large, prominent, highly readable typography matching the article language. Keep all text exact, with no fake characters or pseudo-text. Render as a real PNG image, not SVG or programmatic vector artwork.

ASPECT: 16:9 landscape, medium information density, suitable as a technical article opening illustration.

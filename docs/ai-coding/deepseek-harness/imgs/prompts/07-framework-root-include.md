---
illustration_id: 07
type: framework
style: notion
generator: gpt-image-2
---

标题「根 include：插件树入口」的中文技术框架图。

ZONES:
- 左侧输入组：文件卡 `cordis.yml` 与四张小 `patches` 卡。
- 中央核心卡 `id='include'`，副标题 `cordis:include`，旁边两枚小内建标签 `cordis:include` 与 `cordis:group`。
- 右侧是从核心卡展开的三层插件树圆角节点。
LABELS: 精确渲染标题「根 include：插件树入口」；「id='include'」「cordis:include」「cordis:group」「EntryOptions」「patches」；提示「根条目把配置和 patch 展开成树」。
COLORS: 白底，近黑手绘轮廓；输入淡黄、中心淡蓝、插件节点淡粉和薄荷绿。
STYLE: GPT Image 2 生成真实 PNG；Notion 风格框架图，圆角知识卡、细连接线、极简树图、大量留白；所有标签必须可读，禁止伪文字和水印。
ASPECT: 16:9，横向。

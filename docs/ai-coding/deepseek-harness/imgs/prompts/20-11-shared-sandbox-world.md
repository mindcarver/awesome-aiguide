---
illustration_id: 20-11
type: boundary
style: notion
generator: gpt-image-2
---

Notion 风格边界图，标题 `文件系统与 bash 共享执行世界`。大圆角沙箱边界内有 `ctx.fs` 和 `bash` 两个并列卡；边界入口处是 `审批`，外侧是被拒绝的路径。底部并列错误标签：`FS_SANDBOX_DENIED`、`FS_PERMISSION_DENIED`。表达两种能力必须接受同一隔离和升级规则。白底、浅蓝黄粉、无 logo。

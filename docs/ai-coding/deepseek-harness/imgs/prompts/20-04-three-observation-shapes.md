---
illustration_id: 20-04
type: comparison
style: notion
generator: gpt-image-2
---

Notion 风格三栏对照图，标题 `读取有三种形态`。三张卡依次为：`完整读取`（文件全文和 `version`），`窗口读取`（几行高亮和同一个 `version`），`不存在`（空文件框、`absent` 与 `version`）。底部一句：`观察结果不只有内容，也包括状态与新鲜度`。白色留白、近黑手绘线、淡蓝淡黄淡粉。

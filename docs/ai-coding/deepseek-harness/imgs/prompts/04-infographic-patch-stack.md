---
illustration_id: 04
type: infographic
style: notion
generator: gpt-image-2
---

标题「Patch 栈：由下到上」的中文信息图。

ZONES:
- 中央为四层从下到上略微错开的半透明圆角卡片堆栈，层次清晰：底层 `bundle`，再是 `profile`、`home`、顶层 `overlay`。
- 左侧一根向上的优先级箭头，顶部输出卡 `composeEntries → 最终 entries`。
- 用极简叠层图标表现覆盖关系，而非代码截图。
LABELS: 精确渲染标题「Patch 栈：由下到上」；「bundle」「profile」「home」「overlay」「composeEntries」；提示「越靠上，覆盖优先级越高」。
COLORS: 白底、近黑描边；四层依次柔和蓝、黄、粉、薄荷绿；顶部输出使用淡紫色。
STYLE: GPT Image 2 生成真实 PNG；Notion 风格编辑信息图，手绘细线与模块化卡片，大留白，标签大而清晰；无伪文字、无水印。
ASPECT: 16:9，横向。

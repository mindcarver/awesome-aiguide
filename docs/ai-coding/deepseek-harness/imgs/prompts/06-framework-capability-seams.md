---
illustration_id: 06
type: framework
style: notion
generator: gpt-image-2
text_mode: required
---

能力接缝：换 provider，换整个产品 — Conceptual Framework

Layout: A left-to-right framework on a wide 16:9 canvas. On the left, show the three roles of a capability seam as stacked cards. In the center, show a large seam / adapter junction. On the right, show two alternative execution worlds—local and remote/E2B—receiving the same connected capabilities.

ZONES:
- Left zone: three clear cards labeled “Service Definition”, “Service Provider”, and “Consumer”, connected top-to-bottom as interface → implementation → model-facing tool.
- Center zone: a large replaceable seam with three plug labels “ctx.llm”, “ctx.fs”, and “ctx.sandbox”. Show the plugs as interchangeable modules, not fixed code.
- Right zone: two simple execution-world cards labeled “local” and “remote / E2B”. Each receives the same capability connectors. Add small doodles for files, shell, LSP, and sandbox without extra labels.
- Bottom strip: a concise conclusion ribbon that visually spans both worlds.

RELATIONSHIPS: Make it clear that replacing a provider at the seam reroutes the whole shared execution world. Keep the diagram conceptual and uncluttered; do not draw a detailed network topology.

LABELS: Title: “能力接缝：换 provider，换整个产品”. Labels: “Service Definition”, “Service Provider”, “Consumer”, “ctx.llm”, “ctx.fs”, “ctx.sandbox”. Takeaway: “一个 provider 的替换，移动整个产品”. Render exact supplied labels only; do not invent pseudo-text.

COLORS: Pure white background (#FFFFFF); near-black outlines and text (#1A1A1A); pastel blue (#A8D4F0) for definitions and local execution; pastel yellow (#F9E79F) for the central seam; pastel pink (#FADBD8) for provider and remote/E2B execution; dark gray (#4A4A4A) connectors.

STYLE: GPT Image 2 illustration in a polished Notion-like editorial framework style. Clean hand-drawn lines with slight wobble, modular rounded cards, simple conceptual plug and tool doodles, generous whitespace, restrained pastel accents, no texture, no gradients, no logos, and no photorealism. Text is required and must be exact, large, prominent, readable, article-specific, and free of fake characters or pseudo-text. Render as a real PNG image, not SVG or programmatic vector artwork.

ASPECT: 16:9 landscape, medium information density, suitable for a technical architecture explainer.
